# Agora 사용 안내

이 문서는 Agora를 처음 쓰는 분을 위한 안내입니다. 첫 프로젝트를 만든 뒤 AI에게 질문하고, 토론과 전문 실행을 사용하는 순서로 설명합니다.

> 아직 설치하지 않았다면 [설치 가이드](설치-가이드.md)에서 프로그램 설치와 AI 로그인을 먼저 마치세요.

## Agora에서 할 수 있는 일

Agora에서는 Claude, GPT, Gemini를 한 화면에서 불러 대화할 수 있습니다. 한 AI에게 질문하거나 여러 AI의 답변을 비교할 수 있고, 답변을 서로 검토하게 할 수도 있습니다.

프로젝트 폴더를 연결하면 AI가 사용자가 허용한 범위에서 파일을 읽거나 수정합니다. 작업이 복잡할 때는 기획, 구현, 검수, 기록을 서로 다른 역할로 나눠 진행할 수 있습니다.

| 화면의 참가자 | 부르는 이름 | 연결되는 도구 |
|---|---|---|
| Claude | `@claude` | Claude Code |
| GPT | `@gpt` 또는 `@codex` | Codex CLI |
| Gemini | `@gemini` 또는 `@agy` | Antigravity |

사용 가능한 CLI가 참가자 목록에 표시됩니다. 실제 응답을 받으려면 해당 CLI에 로그인되어 있어야 합니다. 한 가지만 연결해도 일반 대화는 가능하지만, AI 간 토론은 사용할 수 있는 AI가 두 명 이상이어야 합니다.

## 먼저 해볼 세 가지

처음부터 모든 기능을 익힐 필요는 없습니다.

| 하려는 일 | 시작 방법 |
|---|---|
| 여러 의견을 비교하고 싶다 | `@모두`로 질문합니다. |
| 한 AI의 답변을 검토받고 싶다 | 답변 아래의 **[다른 AI에게 전달]**을 누릅니다. |
| 파일 작업을 단계별로 맡기고 싶다 | 프로젝트를 설정한 뒤 **전문 모드**에서 PLAN 또는 전체 실행을 사용합니다. |

## 화면 구성

Agora 화면은 크게 세 부분으로 나뉩니다.

1. **왼쪽 가장자리의 참가자 아이콘**  
   각 AI에서 사용할 모델과 추론 수준을 고릅니다.

2. **왼쪽 사이드바의 프로젝트 목록**  
   프로젝트와 대화를 관리합니다. 프로젝트마다 작업 폴더, 기본 권한, AI 역할을 따로 설정할 수 있습니다.

3. **오른쪽 위의 일반·전문 모드 전환 버튼**  
   자유롭게 대화하려면 일반 모드, 기획과 검수가 필요한 파일 작업이라면 전문 모드를 사용합니다.

## 첫 프로젝트 만들기

파일을 읽거나 수정하는 작업은 프로젝트 폴더를 연결한 뒤 시작하세요.

1. 왼쪽 사이드바에서 **[+ 프로젝트]**를 누릅니다.
2. 프로젝트 이름을 입력합니다.
3. **[폴더 선택]**을 눌러 AI가 작업할 폴더를 고릅니다.
4. **[만들기]**를 누릅니다.
5. 프로젝트 이름 옆의 **[…]**를 눌러 **프로젝트 설정**을 엽니다.
6. **새 대화 기본 권한**을 고릅니다.

| 권한 | AI가 할 수 있는 일 |
|---|---|
| **대화만** | 파일을 읽거나 수정하지 않습니다. |
| **워크스페이스 읽기** | 연결한 폴더 안의 파일을 읽고 검색합니다. |
| **워크스페이스 쓰기** | 연결한 폴더 안의 파일을 만들거나 수정하고 명령을 실행할 수 있습니다. |

전문 모드를 사용하려면 같은 설정 화면의 **전문 모드 역할 설정**에서 기획, 구현, 검토, 기록 담당자를 지정하세요. 기획 검수 담당자를 비워 두면 검토 담당자가 기획도 검수합니다.

## 1. 일반 모드

일반 모드는 메신저처럼 질문하고 답변받는 공간입니다.

### 한 AI에게 묻기

입력창에 `@`를 입력하면 참가자 목록이 나타납니다. 원하는 AI를 선택한 뒤 질문하세요.

```text
@gemini 이 표의 내용을 쉽게 정리해 줘
```

### 모두에게 묻기

`@모두`를 입력하면 현재 연결된 AI 전체가 답변합니다.

| 답변 방식 | 어떻게 답하나요? | 이럴 때 알맞습니다 |
|---|---|---|
| **이어 발언** | 앞선 답변을 읽고 내용을 보완합니다. | 의견을 모아 하나의 결론으로 정리하고 싶을 때 |
| **독립 발언** | 다른 AI의 답변을 보지 않고 각자 답합니다. 순서를 기다리지 않고 **동시에** 시작합니다. | 서로 영향을 받지 않은 답변을 비교하고 싶을 때 |

독립 발언은 여러 AI가 같이 실행되므로 답변이 도착하는 순서가 매번 다를 수 있습니다.
워크스페이스 쓰기 권한에서 독립 발언으로 작업을 시키면, 각 AI에게 **자기 이름의
폴더 안에서만 파일을 만들라고** 안내합니다(예: `claude/`, `codex/`, `agy/`).
같은 파일을 동시에 고쳐 서로의 결과를 덮어쓰지 않도록 하기 위한 것으로, 각자
따로 시안을 만들어 비교할 때 쓰기 좋습니다. 하나의 결과물을 함께 고치는 작업은
이어 발언이나 전문 모드로 진행하세요.

이 폴더 규칙은 **안내이지 차단이 아닙니다.** AI가 규칙을 벗어나 다른 파일을 고칠
수도 있으므로, 실행이 끝나면 Agora가 담당자 폴더 밖에서 바뀐 파일을 찾아 알려
줍니다. 그런 안내가 뜨면 서로의 작업을 덮어썼을 수 있으니 해당 파일을 확인해
주세요. 실행 전부터 여러분이 고쳐 두었던 파일은 이 안내에 포함되지 않습니다.

### AI끼리 토론시키기

**[토론 시작]**을 누르면 AI들이 서로의 의견을 검토합니다. **자유토론**은 발언 수를 정하고 참가자가 차례로 답하며, 합의에 도달하거나 예산을 모두 쓰면 멈춥니다. **구조화 토론**은 기획·Grill·Red Team 같은 Preset을 골라 역할과 순서를 정하고 지정한 사이클만큼 진행합니다.

선택지가 많아 방향을 정하기 어렵거나, 한 답변만으로 결정하기 불안할 때 사용해 보세요.

### 역할이나 팀에 상담하기

프로젝트에 역할 담당자를 지정했다면 다음처럼 부를 수 있습니다.

| 입력 예 | 동작 |
|---|---|
| `@검토자 이 계획의 빠진 조건을 찾아줘` | 검토 담당자 한 명이 상담합니다. |
| `@팀 이 구조를 검토해줘` | 기획자 → 검토자 → 구현자가 차례로 상담합니다. |
| `@팀 실행 설정 파일 검증 로직을 추가해줘` | 기획·기획 검수를 진행하고 구현 직전 승인 대기에서 멈춥니다. |

역할·팀 상담은 파일을 수정하지 않습니다. `@팀 실행`도 구현을 시작하려면 **실행**을 눌러야 합니다. `@모두`는 일반 참가자 전체에게 질문하는 기존 기능입니다. 팀 상담에는 기획·검토·구현 담당자가, 팀 실행에는 기획 검수의 대체 담당자와 기록 담당자를 포함한 실행 역할이 필요합니다.

### 답변을 다른 AI에게 넘기기

AI 답변 아래에는 다음 버튼이 있습니다.

- **[다른 AI에게 전달]**: 답변을 다른 AI에게 보내 검토를 맡기거나 다음 작업을 이어갑니다.
- **[쉽게 설명]**: 전문적인 답변을 일상적인 표현으로 다시 풀어줍니다.

## 2. 전문 모드

전문 모드는 개발이나 문서 정리처럼 파일을 다루는 작업에 알맞습니다. 작업을 다섯 역할로 나눠 진행하며, 필요에 따라 질문에 답하거나 기획·구현을 보완합니다.

기획과 검토를 서로 다른 AI나 모델에 맡기면 같은 관점에서 놓친 부분을 다시 살펴보는 데 도움이 됩니다.

| 역할 | 하는 일 |
|---|---|
| **기획(Planner)** | 작업 목표와 범위를 정리해 작업 지시서(`TASK.md`)를 만듭니다. |
| **기획 검수(Plan Reviewer)** | 지시서에 빠진 조건이나 모순이 없는지 확인합니다. |
| **구현(Builder)** | 승인된 지시서를 기준으로 파일을 수정합니다. |
| **구현 검수(Reviewer)** | 실제 변경 파일과 검증 결과를 보고 완료 여부를 판단합니다. |
| **기록(Recorder)** | 작업 내용과 검수 결과를 프로젝트 기록으로 남깁니다. |

화면은 현재 누가 무엇을 하는지, 왜 대기하는지, 사용자가 다음에 무엇을 해야 하는지를 표시합니다. 역할 목록은 진행률이 아니며, 보완이나 승인 대기로 앞선 역할을 다시 실행할 수 있습니다. 역할 간 전달 요청도 정해진 실행 규칙 안에서 처리합니다.

Reviewer에게는 Builder의 작업 완료 주장 대신 동결된 작업 지시서, 실제 변경 내용, 실행 상태와 검증 근거가 제공됩니다. 이 구조는 구현자의 자기보고에만 의존할 위험을 줄입니다.

### 실행 버튼

| 버튼 | 진행 범위 | 언제 사용하나요? |
|---|---|---|
| **PLAN** | 기획과 기획 검수까지 진행한 뒤 대기 | 계획을 직접 확인하고 나서 실행하고 싶을 때 |
| **실행** | 승인된 계획으로 구현, 검수, 기록을 진행 | 이미 검수를 통과한 계획을 실행할 때 |
| **전체 실행** | 기획 검수 통과 후 구현·검수·기록까지 이어서 진행 | 계획 확인 뒤 구현을 시작하는 권한까지 미리 맡길 때 |

PLAN에는 기획 담당자와 기획 검수 담당자가 필요합니다. 실행에는 구현 담당자와 검토 담당자가 필요하며, 전체 실행에는 기록 담당자까지 지정해야 합니다.

전체 실행에서도 사용자 질문, 완료 전 확인 항목, 작업이 막힌 상태에서는 멈춥니다. 기획 검수 통과가 필요한 승인이나 결정을 모두 대신하지는 않습니다.

PLAN 단계는 파일을 읽을 수 있는 권한으로 진행합니다. Builder가 실제 파일을 수정하려면 워크스페이스 쓰기 권한이 필요합니다. Git이 연결된 워크스페이스에서는 Builder가 작업하기 전에 체크포인트를 만들고, 문제가 생기면 작업 전 상태로 복원할 수 있습니다. Git이 아닌 폴더에서는 자동 복원을 지원하지 않으므로 중요한 파일을 따로 백업한 뒤 실행하세요.

### 사용자 답변을 기다릴 때

작업에 사용자의 결정이 필요하면 Agora가 진행을 멈추고 질문을 표시합니다. 답변을 입력하면 다음 단계가 이어집니다.

기획 검수를 통과한 뒤(**실행을 기다리는 상태**)에도 입력칸은 열려 있습니다. 이때
적는 내용은 일반 대화가 아니라 **기획 수정 요청**으로 전달되어 기획부터 다시
진행합니다. 그대로 진행하려면 **[실행 ▶]**을 누르세요.

### 완료 전에 확인할 항목이 있을 때

구현과 검수가 끝나도 사람이 확인해야 하는 항목이 있으면 입력창 위에 목록이 나타납니다. 각 항목의 내용과 확인이 필요한 이유를 읽고 **승인** 또는 **거부**를 누르세요. 모든 필요한 승인이 끝나면 기록 단계로 이어집니다. 승인하지 않은 항목이 남아 있는 동안에는 실행이 완료되지 않으며, 화면이 먼저 완료로 바뀌는 일은 없습니다.

거부하면 완료로 처리하지 않고 **다음 처리 선택**에서 변경 유지·복원·재기획 방법을 고르게 됩니다. 현재 대화의 현재 실행에 속한 항목만 표시하며, 다른 대화로 옮기거나 새 실행을 시작하면 이전 목록이 사라집니다.

| 표시되는 버튼 | 누를 때 |
|---|---|
| **목록 다시 불러오기** | 확인 항목을 불러오는 데 실패했을 때 |
| **실행 취소** | 확인할 항목이 없는데 대기가 풀리지 않을 때. 파일 변경은 그대로 남습니다 |
| **기록 이어서 진행** | 승인은 반영됐지만 이후 기록 단계로 이어지지 못했을 때 |
| **기록 다시 생성** | 기록 담당자가 결과를 정리하는 데 실패했을 때 |

기록 재시도는 이미 처리한 승인을 다시 요구하지 않습니다. 기록에 실패한 실행은 재시도 전까지 완료로 표시하지 않습니다.

## 실행이 멈추거나 방향을 바꾸고 싶을 때

### `BLOCKED`가 표시될 때

Builder가 작업을 끝낼 수 없거나 상태를 확정하지 못하면 실행을 멈춥니다. 화면에는 상황에 따라 다음 선택지가 나타납니다.

- **변경 유지 후 재기획**: 지금까지의 변경을 남겨 둔 채 막힌 이유를 기획자에게 전달합니다.
- **작업 전 복원 후 재기획**: Git 체크포인트로 돌아간 뒤 다시 기획합니다.
- **작업 지시서 직접 수정**: `TASK.md`를 열어 빠진 조건을 직접 보완합니다.
- **현재 변경만 유지(종결)**: 수정된 파일을 남기고 이번 실행을 끝냅니다.
- **작업 전으로 복원(종결)**: Git 체크포인트로 돌아간 뒤 실행을 끝냅니다.
- **작업 폐기**: 작업 전 상태로 복원하고 작업 지시서도 폐기합니다.

복원과 폐기처럼 체크포인트가 필요한 선택지는 실제로 복원 가능한 백업이 있을 때만 사용할 수 있습니다. Git 저장소여도 백업을 만들지 못했거나 더 이상 사용할 수 없으면 비활성화됩니다. 버튼에 마우스를 올리면 이유를 볼 수 있습니다.

### 다시 기획하고 싶을 때

실행이 멈췄거나 사용자 결정을 기다리는 상태에서는 **[PLAN]**으로 다시 기획할 수 있습니다. Builder가 작업 중일 때는 PLAN을 바로 시작할 수 없으므로 먼저 실행을 중지하거나 현재 단계가 끝날 때까지 기다리세요.

### 실행 중 메모를 남기고 싶을 때

오른쪽 위에서 **일반 모드**로 전환하면 현재 실행을 끊지 않고 메모를 남길 수 있습니다. 이 메모는 진행 중인 단계에 끼어들지 않으며, 기획자가 다음 라운드에서 참고합니다.

현재 작업을 멈추려면 **[중지]**를 누르세요.

## 알아두면 좋은 설정

### 모델과 추론 수준

왼쪽의 참가자 아이콘을 누르면 모델과 추론 수준을 바꿀 수 있습니다. 추론 수준을 높이면 더 오래 검토할 수 있지만 응답 시간이 늘고 사용량이 더 들 수 있습니다.

앱은 CLI 버전·로그인을 1시간마다 다시 확인합니다. 모델 목록은 6시간의 캐시 유효기간이 지나거나 CLI가 바뀌면 갱신하며, 조회 중에는 마지막 목록을 먼저 표시합니다. 설정의 **CLI 다시 탐지**를 누르면 즉시 재조회합니다. 새 목록을 보려면 열려 있던 모델 선택창을 닫았다가 다시 여세요.

Codex는 계정에 제공되는 모델 목록을, AGY는 `agy models`가 보고하는 목록을 가져옵니다. Claude는 CLI 도움말에 있는 별칭을 갱신하므로 모든 고정 버전 이름을 자동으로 나열하지 않습니다. 조회 실패 시 기존 목록을 유지하며, 새 모델의 추론 수준 지원 여부가 확인되지 않으면 추측해서 옵션을 붙이지 않습니다.

### 남은 사용량

왼쪽 아래에서 각 AI의 남은 사용량을 확인할 수 있습니다. 토론과 전문 실행은 AI를 여러 번 호출하므로 일반 질문보다 사용량이 빨리 줄 수 있습니다.

한 AI의 한도가 끝나도 일반 대화에서는 다른 AI를 사용할 수 있습니다. 전문 실행에서 해당 AI가 역할을 맡고 있다면 프로젝트 설정에서 담당자를 바꿔야 합니다.

### 작업 기록 위치

Agora의 대화 기록과 첨부 파일은 기본적으로 `%USERPROFILE%\.agora\`에 저장됩니다.

전문 모드를 사용하면 연결한 작업 폴더에 `.project-memory`가 만들어질 수 있습니다. 이 폴더에는 작업 지시서와 실행 기록이 들어 있으므로 진행 중인 작업이 있다면 임의로 삭제하지 마세요.

질문과 작업 내용은 사용자가 고른 AI의 공식 CLI를 거쳐 해당 AI 서비스로 전달됩니다. 민감한 정보를 보내기 전에는 연결한 서비스의 데이터 처리 정책과 프로젝트 폴더의 내용을 확인하세요.

## 자주 묻는 질문

| 문제 | 확인할 내용 |
|---|---|
| 특정 AI가 참가자 목록에 없습니다 | 해당 CLI가 설치되어 있는지와 로그인이 끝났는지 확인하세요. |
| `@`를 입력해도 참가자 목록이 나오지 않습니다 | 입력창을 한 번 클릭해 커서를 둔 뒤 다시 `@`를 입력하세요. |
| 토론 버튼이 비활성화되어 있습니다 | 토론에는 사용할 수 있는 AI가 두 명 이상 필요합니다. |
| 전문 모드 버튼이 회색입니다 | 필요한 역할이 지정되어 있는지, 다른 응답이나 실행이 진행 중인지 확인하세요. 실행 버튼은 기획 검수를 통과한 뒤 활성화됩니다. 버튼 위에 마우스를 올리면 현재 필요한 조건이 표시됩니다. |
| AI가 프로젝트 파일을 찾지 못합니다 | 프로젝트 설정에서 올바른 폴더를 연결했는지와 읽기·쓰기 권한을 확인하세요. |
| 응답이 멈추거나 오래 걸립니다 | 사용량과 네트워크 상태를 확인하세요. 필요하면 **[중지]**를 누른 뒤 다시 시도합니다. |
| 검수가 끝났는데 완료되지 않습니다 | 확인할 항목이 남았는지 확인하세요. 승인 뒤 재개 실패는 **기록 이어서 진행**, 기록 실패는 **기록 다시 생성**으로 처리합니다. |
| 복원 버튼이 회색입니다 | 현재 실행에 사용 가능한 체크포인트가 없습니다. 변경을 유지하는 선택지를 사용하세요. |
| 새 모델이 보이지 않습니다 | **CLI 다시 탐지** 후 모델 선택창을 다시 여세요. CLI나 계정에서 제공하지 않는 항목은 자동으로 추가되지 않습니다. |

## 첫 질문 보내기

일반 모드에서 아래 문장을 입력해 보세요.

```text
@모두 너희가 함께 검토하면 좋은 일과 각자 잘하는 일을 알려 줘.
```

설치 방법은 [설치 가이드](설치-가이드.md)에서 다시 볼 수 있습니다. 전체 기능은 [README](README.md), 기술 문서와 팀 확인 항목은 [문서 안내](docs/README.md)에서 확인하세요.
