# Ἀγορά Phase 1 검수 결과 및 수정 계획

본 문서는 2fe495 커밋을 기준으로 수행한 전체 감사 결과를 바탕으로 작성되었습니다. 앱의 핵심 기능은 안정적이나(테스트 353개 통과), 실제 환경과 데이터 안전성 측면에서 보완이 필요한 항목들을 정리했습니다.

## 직접 재현하여 확인한 주요 문제점

| # | 문제 | 확인된 증상 |
|---|---|---|
| 1 | 프로젝트 삭제 | 메모리·규칙 파일 2개가 디스크에 남는데 앱에서 접근 불가. 기본 프로젝트로 안 옮겨짐 |
| 2 | 맥락 예산이 정확히 0 | 요약을 빼야 할 상황에 5만 자 전부 들어감. 프롬프트 1.6만 → 6.6만 자 |
| 3 | 전문 모드 끼어들기 | 순서가 \구현 → 검토 → 일반 → 일반 → 기록\. 잡담이 기록관 요약에 포함되고, 끼어든 답변도 파일 수정 권한을 물려받음 |
| 4 | 기획자 역할 | 설정 화면에는 있는데 실행 파이프라인에 연결 안 됨. 지정해도 무시됨 |
| 5 | 결정·작업 수정 | 수정해도 "수정 시각"이 갱신 안 됨 (1000 → 1000). 목록 정렬이 틀어짐 |
| 6 | 기록관 실패 | 실패했는데 "기록관이 결과를 정리했습니다"라고 표시 |
| 7 | 전문 모드 중 중지 | 전문 모드와 토론이 동시에 요청 가능한 상태 |
| 8 | 개인 경로 노출 | \BASELINE_AUDIT.md\에 사용자 폴더 경로 3줄 노출 |

## 개선 우선순위

### P1. 데이터를 잃을 수 있는 문제 (최우선)

- **읽기 전용 상태 무시:** 화면에 '읽기 전용'으로 표시되어도 메모리 추가 및 프로젝트 삭제가 실행됨. 대화가 삭제된 프로젝트를 계속 참조하는 문제 발생.
- **손상된 프로젝트 처리:** 프로젝트 파일 하나가 손상되면 대화가 전부 "분류되지 않음"으로 이동. 기본 프로젝트 파일 손상 시 새 기본값으로 덮어써 데이터 유실 위험.
- **저장 실패 재시도:** 결정·작업 저장이 실패한 후 재시도 시 "이미 있는 내용"으로 간주하여 파일 기록 생략. 화면에는 성공으로 보이나 재시작 시 사라짐.
- **원자적이지 않은 규칙 저장:** 규칙 파일 덮어쓰기 중 강제 종료 시 파일 내용이 비워질 위험. 임시 파일 작성 후 교체 방식 적용 필요.
- **동시 접근 방지:** 동일 데이터 폴더 다중 접근 시 늦은 쪽 데이터만 남음. 현재의 중복 실행 방지 기능에 대한 실질적인 동작 테스트 부재.
- **프록시 복구 실패:** Codex 프록시 복구 실패 시 설정이 계속 '사용' 상태로 남아 이후 호출 영구 실패 유발.

### P2. 대화 품질에 영향을 주는 문제

- **프롬프트 격리 부재:** 턴 예약 후 들어온 질문까지 모두 읽어들여 엉뚱한 답변 유발. 각 턴은 '예약된 시점까지의 대화'만 읽도록 수정 필요.
- **상태 동기화 지연:** 결정이나 작업을 저장해도 현재 열려 있는 대화창의 에이전트는 알지 못함. 창을 다시 열거나 새로고침해야 반영됨.
- **AGY 프롬프트 잘림:** 대화가 길어질 경우 AGY 프롬프트 앞부분이 잘리면서 프로젝트 개요 및 확정된 결정까지 삭제됨.
- **Codex 모델 조회 실패:** 모델 목록 조회 실패 시 \--model unknown\으로 실행되어 호출 실패.
- **승인 API 취약점:** 오타(\ction: \"typo\"\)도 승인으로 처리. 확정된 결정도 상태 되돌리기 가능.
- **워크플로 연결 오류:** 대화를 다른 프로젝트로 옮길 때 결정·작업의 연결 참조가 깨짐.
- **기록관 오류 은폐:** 결정·작업 후보 등록 실패 시 에러를 조용히 무시하고 성공으로 표시.
- **역할/모델 기록 부재:** 전문 모드 실패 시 어떤 역할과 모델이 실패했는지 화면에 나타나지 않음.
- **메모리 증가 (긴 답변):** 스트리밍 답변 시 중간 텍스트를 무제한으로 쌓아 메모리 문제 유발 가능.

### P3. 사용성 저하 및 버그

- 전송 실패 시 작성 중이던 지시문 유실.
- 첨부 파일을 전송 전에 지워도 복사본이 디스크에 남아 세션 용량 한도 차지.
- 프로젝트 설정 작성 중 "폴더 선택" 시 작성 중이던 내용 초기화.
- 첨부 처리 중 대화 탭 이동 시 화면 상태 꼬임.
- "제외"된 결정은 화면에서 사라져 복원 불가 (작업 목록과 동작 다름).
- 권한 승인 창이 떠도 키보드 포커스가 차단되지 않아 배경의 채팅창에 입력됨.
- 5~20MB 이미지 첨부 시 미리보기 창이 빈칸으로 나타남 (첨부는 정상).
- 중요한 저장소 경고가 5초 뒤 일반 알림과 함께 사라짐.
- 파일 11개 이상 드래그 앤 드롭 시 안내 없이 10개만 처리.

### 기타 검토 사항 (회사 배포 관련)

- **로그인 정보:** \.agora\ 내부에 토큰 저장. \.gitignore\ 누락 시 Git에 올라갈 위험.
- **설정 분리:** 채팅/프로젝트는 \~/.agora\, 기타 설정은 \%APPDATA%\\Agora\에 나뉘어 저장됨. 이전 시 주의 필요.
- **Windows 서명:** 서명이 없어 보안 정책에 의해 실행 차단될 가능성.
- **배경 폴더 감시:** 펫 기능을 꺼도 1.5초 간격으로 폴더 훑기 계속 실행.
- **오류 표시 부재:** 펫을 끈 상태에서는 계정 오류의 구체적인 이유 확인 불가.
- **사용량 조회 지연:** 색상 설정 시에도 네트워크 사용량 조회를 대기하여 화면 지연 발생.

## 작업 진행 제안

1.  **데이터 무결성 및 파이프라인 (추천 1순위):**
    *   데이터 손실 문제(읽기 전용 무시, 저장 실패 재시도, 규칙 원자적 저장) 수정.
    *   전문 모드 파이프라인 잠금 (끼어들기 방지) 및 기획자 역할(Planning) 연결.
2.  **회사 PC 배포 환경 구성:**
    *   토큰 저장 위치 안내 문서화 및 \.gitignore\ 추가.
    *   Codex 프록시 자동 복구 P1 수정.
    *   긴 출력 시 메모리 증가 문제 수정.
3.  **UI/UX 및 잡다한 개선:**
    *   미사용 코드 정리(CSS 160줄, 중복 함수, 불필요한 KaTeX 등).
    *   전송 실패 시 입력 내용 복원, 드래그 앤 드롭 등 부수적인 오류 수정.
