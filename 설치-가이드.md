# Agora 설치 가이드 (처음 사용자용)

컴퓨터를 잘 몰라도 괜찮습니다. 이 문서의 순서대로 **더블클릭과 클릭만** 하면 됩니다.
명령어를 입력하는 단계는 없습니다.

> 준비물: Windows 10 또는 11 PC, 인터넷 연결. 이것만 있으면 됩니다.

---

## 1단계 — Agora 내려받기

두 가지 방법이 있습니다. **처음이라면 방법 A**를 추천합니다.

### 방법 A. ZIP으로 받기 (가장 쉬움)

1. 인터넷 브라우저에서 Agora GitHub 페이지를 엽니다.
2. 초록색 **`< > Code`** 버튼을 클릭합니다.
3. **`Download ZIP`** 을 클릭합니다. → `Agora-main.zip` 파일이 다운로드됩니다.
4. 다운로드된 ZIP 파일에서 **마우스 오른쪽 버튼 → "압축 풀기"** 를 클릭합니다.
   - ⚠️ **가장 흔한 실수**: 압축을 풀지 않고 ZIP 안에서 바로 더블클릭하면 설치가 안 됩니다. 반드시 먼저 압축을 푸세요.
5. 압축이 풀린 `Agora-main` 폴더를 원하는 곳으로 옮깁니다. 예: `D:\Agora`
   - 폴더 이름과 위치는 아무거나 괜찮습니다.

> 참고: ZIP으로 받은 폴더는 **자동 업데이트가 되지 않습니다.**
> 새 버전이 나오면 ZIP을 다시 받아 폴더를 교체하면 됩니다.
> 자동 업데이트를 원하면 아래 방법 B를 사용하세요.

### 방법 B. Git으로 받기 (자동 업데이트 가능 — 조금 더 익숙한 분용)

1. https://git-scm.com 에서 Git을 내려받아 설치합니다.
   - 설치 중 어려운 질문이 나와도 **전부 그대로 "Next"** 를 누르면 됩니다.
2. 키보드에서 **Windows 키**를 누르고 `cmd` 라고 입력한 뒤 Enter를 누릅니다. 검은 창이 열립니다.
3. 아래 두 줄을 한 줄씩 복사해서 검은 창에 붙여넣고(창 안에서 **마우스 오른쪽 클릭** = 붙여넣기) Enter를 누릅니다.

```text
cd /d D:\
git clone https://github.com/Eachelp/Agora.git
```

4. `D:\Agora` 폴더가 만들어집니다. 검은 창은 닫아도 됩니다.

---

## 2단계 — 설치하기

1. Agora 폴더를 열고 **`Agora-설치하기`** 파일을 **더블클릭**합니다.
2. 파란색 **"Windows의 PC 보호"** 화면이 나올 수 있습니다. 정상입니다.
   - **"추가 정보"** 를 클릭 → **"실행"** 버튼을 클릭하세요.
3. 검은 창이 열리고 컴퓨터 상태를 확인해 줍니다. 이런 식으로 보이면 정상입니다:

```text
============================================
               Agora Setup
============================================

실행 환경을 확인합니다...

[OK] git version 2.55
[OK] Node.js v22
[OK] npm 10
[미설치] Claude Code
...
```

4. **`[미설치]`가 있어도 걱정하지 마세요.** 설치기가 하나씩 물어봅니다.
   - `[Y,N]?` 라는 질문이 나오면: 설치하려면 키보드에서 **Y**, 건너뛰려면 **N** 을 누릅니다.
   - **"이 앱이 디바이스를 변경할 수 있도록 허용하시겠어요?"** 라는 창이 뜨면 **"예"** 를 클릭합니다.
5. **Node.js를 방금 새로 설치했다면**: 화면 안내에 따라 창을 닫고, `Agora-설치하기` 를 **한 번 더 더블클릭**하세요. (새로 설치한 프로그램을 컴퓨터가 인식하는 데 필요한 과정입니다.)
6. "Agora 설치가 완료되었습니다"가 나오고 **바탕화면에 Agora 아이콘**이 생기면 끝입니다.
   아무 키나 누르면 Agora가 바로 실행됩니다.

---

## 3단계 — 실행하기

- 바탕화면의 **Agora** 아이콘을 더블클릭하면 채팅 화면이 열립니다.
- 다음부터는 항상 이 아이콘으로 실행하면 됩니다.

---

## 4단계 — AI 로그인하기

Agora는 세 가지 AI를 연결할 수 있습니다. **전부 필요하지 않습니다.
실제로 사용할 AI만 로그인하면 됩니다.** (각 AI 회사의 계정/구독이 필요합니다)

| AI | 필요한 것 |
|---|---|
| Claude Code | Anthropic(Claude) 계정 |
| Codex | OpenAI(ChatGPT) 계정 |
| Antigravity | Google 계정 + Antigravity 앱 |

로그인 방법:

1. Agora 화면 왼쪽 아래의 **설정(톱니바퀴)** 버튼을 클릭합니다.
2. **"계정"** 탭을 클릭합니다.
3. 사용할 AI의 **"계정 추가"** 버튼을 클릭합니다.
4. 열리는 창의 안내에 따라 로그인합니다. (보통 인터넷 브라우저가 열리고, 해당 AI 계정으로 로그인하면 됩니다)

---

## 업데이트하기

새 버전이 나왔을 때:

- **방법 B(Git)로 받았다면**: Agora 폴더의 **`Agora-업데이트`** 를 더블클릭하면 끝입니다.
  대화 기록과 설정은 그대로 보존됩니다.
- **방법 A(ZIP)로 받았다면**: ZIP을 다시 내려받아 1~2단계를 반복하세요.
  대화 기록과 설정은 Agora 폴더가 아니라 내 문서 쪽(사용자 폴더의 `.agora`)에 저장되므로, 폴더를 교체해도 사라지지 않습니다.

---

## 문제가 생겼을 때

| 증상 | 해결 방법 |
|---|---|
| 파일을 더블클릭했는데 파란 경고 화면이 뜸 | 정상입니다. "추가 정보" → "실행" 클릭 |
| 검은 창이 떴다가 바로 사라짐 | 압축을 풀지 않고 ZIP 안에서 실행했는지 확인하세요. 압축을 먼저 푸세요 |
| Node.js를 설치했는데 계속 "미설치"라고 나옴 | PC를 재부팅한 뒤 `Agora-설치하기` 를 다시 실행하세요 |
| "이 폴더는 ZIP으로 내려받은 폴더라 자동 업데이트를 할 수 없습니다" | 정상 안내입니다. 새 ZIP을 받아 폴더를 교체하거나, 방법 B로 다시 받으면 다음부터 자동 업데이트가 됩니다 |
| 백신 프로그램이 경고함 | Agora는 오픈소스이며 코드가 모두 공개되어 있습니다. 신뢰할 수 있다면 허용을 선택하세요 |
| 설치 중 "인터넷 연결을 확인" 오류 | 인터넷 연결을 확인하고 `Agora-설치하기` 를 다시 실행하세요. 이미 된 부분은 건너뛰므로 여러 번 실행해도 안전합니다 |

그래도 안 되면, 검은 창에 마지막으로 보이는 문구를 사진으로 찍어 문의해 주세요.
